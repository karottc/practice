/* 社会名流问题
 *
 * 参数：know - 保存任务之间的认识相互关系，n x n 的数组;
 * 输出：社会名流
 *
 * 前提：假定最多只有一个社会名流，并且社会名流不认识其他人，其他人都认识
 * 社会名流。
 */

int Celebrity(int **know, int n)
{
    int i = 0, j = 1, next = 3;
    int candidate = 0;
}
